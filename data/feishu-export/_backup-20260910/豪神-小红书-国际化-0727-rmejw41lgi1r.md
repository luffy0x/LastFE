# 豪神——小红书-国际化-0727

**全程AI**

### Rag流程

### 如何开发一个tool

### 如何开发一个skill

### mcp什么作用

### mcp分为client和server，在哪开发tool

### agent的设计范式

## 算法

### Promise输出题

### Promise并发控制

```
/* 题目：实现 pLimit(concurrency)，返回一个函数 limit(fn)，用于限制同时执行的 Promise 任务数量。
要求：
concurrency 为最大并发数（>=1）
limit(() => promise) 返回 Promise<T>
任务按“进入队列顺序”调度
任一任务失败不影响后续任务继续调度（但该任务返回的 Promise 仍 reject）*/
```
