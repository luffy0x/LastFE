# 军子-百度秋招AI全栈一面

1.自己讲述实习和项目  30分钟

2.claude code不是已经有worktree了吗？为什么实习还要使用worktree？

3.如果有三个请求，用户先请求了a，但是a请求却是最后到达的，会导致什么问题吗？怎么解决？

算法和手撕：

1.合并两个有序数组

2.最大并发限制执行任务

3.sleep函数

4.最后打印的是哪个

```
function change(obj) {
  obj.name = "tom";
  obj = {
    name: "join",
  };
}
const obj = {
  name: "son",
};
change(obj);
console.log(obj.name); 
```
