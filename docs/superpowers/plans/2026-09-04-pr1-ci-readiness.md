# PR1 CI Readiness Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make PR #1 pass its application and infrastructure CI gates so it can be marked ready and merged into `main`.

**Architecture:** Align the container base image with the Node 22 runtime used by CI and documented by the project, preserving the existing pinned-image guard. Add CI artifact retention for failed Playwright runs so Linux screenshot actuals can be reviewed and promoted into committed Linux baselines; keep the existing platform-specific snapshot contract.

**Tech Stack:** GitHub Actions, Dockerfile, Node.js 22, pnpm 11.19.0, Next.js, Playwright.

**Spec:** `web/README.md`, `.github/workflows/ci.yml`, and the existing PR #1 CI failure logs.

## Global Constraints

- Preserve the current `main` worktree and all unrelated user changes.
- Do not commit secrets, generated dependency directories, or local databases.
- Keep Docker base images pinned by digest.
- Keep Playwright visual regression checks platform-specific; commit Linux baselines generated by Ubuntu CI rather than renaming Windows images.
- Do not merge until the full GitHub CI workflow is green.

---

### Task 1: Align the Docker runtime with the supported Node version

**Files:**
- Modify: `web/Dockerfile:1`
- Modify: `web/Dockerfile.test.mjs:8-11`

**Interfaces:**
- Consumes: the Node 22 CI runtime and the project README requirement.
- Produces: a pinned Node 22 base image compatible with `better-sqlite3` prebuilds and the existing structural Dockerfile guard.

- [ ] **Step 1: Update the pinned base image**

Replace the Node 24 image reference with the verified Node 22 `bookworm-slim` multi-architecture digest `sha256:83f487e0a63425e5b4d146fb5e5be574bcbe1b7b843d3ebafdd95eaf7767a7e5`.

- [ ] **Step 2: Update the structural guard**

Change the exact `FROM` assertion to require the same Node 22 image and digest.

- [ ] **Step 3: Verify the guard**

Run `node web/Dockerfile.test.mjs` from the repository root and expect exit code 0.

### Task 2: Make failed Linux screenshot output retrievable

**Files:**
- Modify: `.github/workflows/ci.yml` after the Playwright test step.

**Interfaces:**
- Consumes: Playwright's `web/test-results` output when a screenshot baseline is missing or mismatched.
- Produces: a GitHub Actions artifact named `playwright-test-results` retained on failed application runs.

- [ ] **Step 1: Add an always-run artifact upload**

Add `actions/upload-artifact@v4` with `if: ${{ always() }}` and `path: web/test-results`, allowing missing paths, and a short retention period.

- [ ] **Step 2: Verify workflow syntax and scope**

Inspect the changed workflow and run `node web/Dockerfile.test.mjs`; do not weaken or skip the Playwright test command.

- [ ] **Step 3: Push the CI-enablement commit**

Commit only the Docker and workflow changes, push them to `codex/knowledge-frontier`, and wait for the application job artifact.

### Task 3: Promote Ubuntu Playwright actuals into committed baselines

**Files:**
- Create: `web/e2e/map-navigation.spec.ts-snapshots/map-375-chromium-linux.png`
- Create: `web/e2e/map-navigation.spec.ts-snapshots/map-768-chromium-linux.png`
- Create: `web/e2e/map-navigation.spec.ts-snapshots/map-1024-chromium-linux.png`
- Create: `web/e2e/map-navigation.spec.ts-snapshots/map-1440-chromium-linux.png`

**Interfaces:**
- Consumes: actual screenshot files from the Ubuntu CI artifact.
- Produces: the four Linux baselines required by `toHaveScreenshot` in `map-navigation.spec.ts`.

- [ ] **Step 1: Download the failed-run artifact**

Use `gh run download <run-id> --name playwright-test-results` and locate the four `*-actual.png` files under the map snapshot test result directories.

- [ ] **Step 2: Copy actuals to exact baseline paths**

Copy only the four map screenshot actuals to the four `*-chromium-linux.png` paths; do not commit the full test-results directory.

- [ ] **Step 3: Verify locally available checks**

Run `pnpm lint`, `pnpm typecheck`, `pnpm test --run`, and `pnpm exec playwright test` in the worktree. The Windows run must remain green and the Linux baselines must be present for CI.

### Task 4: Ship and integrate

**Files:**
- Commit only files listed in Tasks 1-3.

**Interfaces:**
- Consumes: green local verification and a clean PR branch.
- Produces: an updated PR #1 with passing GitHub checks, marked ready for review, merged into `main`.

- [ ] **Step 1: Inspect the final diff and status**

Run `git status --short --branch -uall`, `git diff --stat origin/codex/knowledge-frontier...HEAD`, and confirm no dependency directories or unrelated files are staged.

- [ ] **Step 2: Commit and push**

Use Conventional Commit messages and push without force; stop if the remote branch moved unexpectedly.

- [ ] **Step 3: Wait for CI and review PR state**

Use `gh pr checks 1` and `gh pr view 1` until both jobs pass and the PR is no longer Draft.

- [ ] **Step 4: Merge PR #1**

Merge the PR into `main` only after checks are green and GitHub reports the merge succeeded; then verify the merge commit/state and local remote-tracking refs.
